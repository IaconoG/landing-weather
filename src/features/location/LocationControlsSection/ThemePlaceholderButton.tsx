const ThemePlaceholderButton: React.FC = () => {
  return (
    <button
      type="button"
      className="location-controls-section__theme-placeholder"
      disabled
    >
      Tema (proximamente)
    </button>
  );
};

export default ThemePlaceholderButton;
